services:
  rpsd-ingest:
    extends:
      file: ./docker-compose-base.yml
      service: rpsd-ingest
    ports:
      - "${APP_EXTERNAL_PORT:-9999}:${APP_PORT:-8000}"
    # DO NOT add env_file or environment sections for Django service
    # Pydantic Settings reads .env directly inside container
